// 这是一个 TypeScript 示例文件
// 用于测试代码行数统计

/**
 * 求和函数
 * @param a - 第一个数
 * @param b - 第二个数
 */
export function add(a: number, b: number): number {
  return a + b;
}

/**
 * 问候函数
 */
export function greet(name: string): string {
  // 返回问候语
  return `Hello, ${name}!`;
}

/* 块注释示例 */
export const PI = 3.14159;

// 单行注释
const DEBUG = true;
